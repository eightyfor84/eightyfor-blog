# Chronicle Aurora — Collection definitions (合集定义)
# A list of collections; each `nodes` entry is a post (`type: post`) or a group of posts.
- name: Chronicle Guide
  description: Everything you need to get started with Chronicle — from first principles to advanced features.
  slug: chronicle-guide
  nodes:
    - id: welcome-to-chronicle
      type: post
    - id: getting-started
      type: post
    - id: ""
      type: group
      title: Configuration
      children:
        - id: site-config
          type: post
        - id: profile-config
          type: post
        - id: c8n-config
          type: post
        - id: friend-config
          type: post
        - id: comments-config
          type: post
    - id: slides-demo
      type: post
    - id: markdown-showcase
      type: post
